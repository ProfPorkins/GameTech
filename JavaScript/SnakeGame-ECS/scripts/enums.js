'use strict';

Demo.enums.Direction = Object.freeze({
    Stopped: 'stopped',
    Up: 'up',
    Down: 'down',
    Left: 'left',
    Right: 'right'
});

Demo.enums.Event = Object.freeze({
    ConsumeFood: 'consume-food',
    HitSomething: 'hit-something'
});

Demo.enums.Input = Object.freeze({
    KeyboardControlled: 'keyboard-controlled'
});
